query CartTransformRunInput {
  cart {
    lines {
      id
      quantity
      attribute(key: "gbi_price") {
        value
      }
      merchandise {
        __typename
        ... on ProductVariant {
          id
        }
      }
    }
  }
}
